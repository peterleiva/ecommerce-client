import { TabTriggerDirective } from './tab-trigger.directive';

describe('TabTriggerDirective', () => {
  it('should create an instance', () => {
    const directive = new TabTriggerDirective();
    expect(directive).toBeTruthy();
  });
});
