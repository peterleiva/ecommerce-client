import { DropMenuDirective } from './drop-menu.directive';

describe('DropdownContentDirective', () => {
  it('should create an instance', () => {
    const directive = new DropMenuDirective();
    expect(directive).toBeTruthy();
  });
});
