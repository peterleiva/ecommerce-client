import { DropMenuTriggerDirective } from './drop-menu-trigger.directive';

describe('DropMenuTriggerDirective', () => {
  it('should create an instance', () => {
    const directive = new DropMenuTriggerDirective();
    expect(directive).toBeTruthy();
  });
});
